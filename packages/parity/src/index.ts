/**
 * @facet/parity — a private test harness (not a surface). It drives one capability through all four surfaces
 * (agent, http, mcp, cli) and asserts they agree: same authz, same confirmation gate, same idempotency, same
 * error taxonomy. This is the proof that "one typed capability → four surfaces" holds with zero per-surface
 * drift.
 *
 * SCAFFOLD STUB — the Harden phase implements this. Kept as an empty module so the baseline is green.
 */
export {};
