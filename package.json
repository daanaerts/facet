{
  "name": "facet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run scripts/check.ts",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "@facet/agent": "workspace:*",
    "@facet/cli": "workspace:*",
    "@facet/core": "workspace:*",
    "@facet/http": "workspace:*",
    "@facet/mcp": "workspace:*",
    "@facet/surface-kit": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
